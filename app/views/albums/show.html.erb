<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @album.title %>
</p>

<%= image_tag @album.image.url(:medium) %>

<p>
  <strong>Description:</strong>
  <%= @album.description %>
</p>

<p>
  <strong>Date:</strong>
  <%= @album.date %>
</p>

<%= link_to 'Add Picture', new_picture_path(album_id: @album) %> |
<%= link_to 'Edit', edit_album_path(@album) %> |
<%= link_to 'Back', albums_path %>
